const Home = () => {

    return (
        <>
            <h1>Home</h1>
            {/* <code>
                <pre>{JSON.stringify(firebase.apps[0].options, null, 2)}</pre>
            </code> */}
        </>
    )
};

export default Home;
